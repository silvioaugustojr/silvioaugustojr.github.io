gem "eventmachine", "1.2.7", platform: :ruby do
  # skip building native extension for local ARM Macs
end

source 'https://rubygems.org'

gem 'github-pages'
gem 'connection_pool', '2.5.0'

group :jekyll_plugins do
  gem 'jekyll-feed'
  gem 'jekyll-sitemap'
  gem 'jekyll-redirect-from'
  gem 'jemoji'
  gem 'webrick', '~> 1.8'
end